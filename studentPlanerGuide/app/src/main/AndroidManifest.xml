<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.studentplanerguide">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StudentPlanerGuide">
        <activity android:name=".mainPages.subjects.SkillsActivity"></activity>
        <activity android:name=".mainPages.subjects.SubjectTopicsActivity" />
        <activity android:name=".mainPages.subjects.HomescreenActivity" />
        <activity android:name=".mainPages.subjects.OnboardingActivity" />
        <activity android:name=".mainPages.subjects.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>